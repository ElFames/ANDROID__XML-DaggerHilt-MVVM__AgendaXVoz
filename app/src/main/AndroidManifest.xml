<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="agenda.x.voz">

    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AgendaXVozApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_agenda_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_agenda_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgendaXVoz"
        tools:targetApi="31">

        <activity
            android:name=".ui.views.MainActivity"
            android:screenOrientation="portrait"
            android:exported="true" />

        <receiver
            android:name=".core.notifications.AlarmNotification"
            android:enabled="true" />

        <activity
            android:name=".ui.views.SplashScreen"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.MySplashTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>